---
title: "Grocery Store Summary"
author: "Vincent && Joseph Scavetta"
output:
  word_document: default
---

```{r}
library(plyr)
scan.data <- read.csv("data.csv")
```

# Number of customers
```{r comment=NA}
cat(scan.data$CustomerNumber[nrow(scan.data)])
```


# Total Sales
```{r comment=NA}
cat(sum(scan.data$SalePrice))
```


# Total Items bought
```{r comment=NA}
cat(nrow(scan.data))
```


# Top 10 selling items with counts
```{r comment=NA}
purchase.count <- count(scan.data, "ProductPurchased")
purchase.count[order(purchase.count$freq, decreasing = TRUE)[1:10],]
```